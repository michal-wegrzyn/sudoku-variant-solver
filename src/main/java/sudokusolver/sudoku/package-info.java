/**
 * Models the core domain, structural components, and high-level abstractions of
 * a Sudoku puzzle.
 * <p>
 * This package encapsulates the behavioral state of the puzzle, including the
 * grid matrix ({@link sudokusolver.sudoku.Board}), cellular geometry
 * ({@link sudokusolver.sudoku.Cell}), ASCII-art parser
 * ({@link sudokusolver.sudoku.RegionSelector}), and the
 * primary orchestrator ({@link sudokusolver.sudoku.Sudoku}) that binds game
 * logic to the CSP solver.
 * </p>
 */
package sudokusolver.sudoku;